eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    registry-fetch-interval-seconds: 10
    service-url:
      defaultZone: http://localhost:9001/eureka/
server:
  port: 9100
spring:
  application:
    name: order

#开启Hystrix
feign:
  hystrix:
    enabled: true